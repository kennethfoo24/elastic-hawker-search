apiVersion: v1
kind: Service
metadata:
  name: hawker-search
  labels:
    app: hawker-search
spec:
  type: LoadBalancer
  selector:
    app: hawker-search
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
